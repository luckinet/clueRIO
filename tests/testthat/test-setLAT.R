library(testthat)
library(checkmate)

test_that("the table contains all values in the initial landuse map", {

})

test_that("the table contains no more values than in the initial landuse map", {

})

